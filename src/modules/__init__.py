"""Bounded-context modules: bi, assistant, crm, wms, catalog.

Phase 0 scaffolds only — implementations grow per module without
importing each other's internals (use depotru_kernel + depotru_tools).
"""
